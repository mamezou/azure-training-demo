# コントリビューションガイド

azure-training-demoへのコントリビューションをお考えいただき、ありがとうございます！

## コントリビューションの方法

### 新しいデモシナリオの追加

1. **Issue を作成**: まず、追加したいデモシナリオの概要をIssueで提案してください
2. **フォークとブランチ**: リポジトリをフォークし、作業用ブランチを作成してください
3. **ディレクトリ構成**: 適切なトレーニングコースのディレクトリに追加してください
4. **ドキュメント**: README.mdを更新し、新しいシナリオを追加してください
5. **プルリクエスト**: 変更をコミットし、プルリクエストを作成してください

### 既存デモの改善

- 手順の明確化
- エラーの修正
- ベストプラクティスの追加
- スクリーンショットの追加

## ディレクトリ構造

新しいデモシナリオを追加する際は、以下の構造に従ってください：

### Terraformデモ

```
AZ[試験番号]/terraform/[シナリオ名]/
├── main.tf              # メインのTerraform設定
├── variables.tf         # 変数定義
├── outputs.tf          # 出力定義
├── terraform.tfvars.example  # 変数の例（実際の値は含めない）
└── README.md           # シナリオの説明と使用方法
```

### 手動デモ

```
AZ[試験番号]/manual-demos/[番号]-[シナリオ名].md
```

手順書のフォーマット：

```markdown
# [タイトル]

## 概要
デモの目的と学習内容

## 前提条件
- 必要な権限
- 事前に準備するもの

## 所要時間
XX分

## 手順

### ステップ 1: [ステップタイトル]
詳細な手順...

### ステップ 2: [ステップタイトル]
詳細な手順...

## 確認事項
正しく実行できたかの確認方法

## クリーンアップ
リソースの削除手順

## 補足説明
関連する概念やベストプラクティス
```

## コーディング規約

### Terraform

- Terraform 1.0以降の構文を使用
- リソース名は小文字とハイフンを使用（例: `resource-group-demo`）
- 変数にはデフォルト値と説明を追加
- センシティブな値は変数として定義し、`.tfvars.example`でサンプルを提供

```hcl
variable "location" {
  description = "Azure region for resources"
  type        = string
  default     = "japaneast"
}
```

### ドキュメント

- 日本語で記載
- コマンドは実行可能な形式で記載
- スクリーンショットを含める場合は、機密情報が含まれていないことを確認
- 専門用語には必要に応じて説明を追加

## テスト

新しいTerraformテンプレートを追加する場合：

1. `terraform init`、`terraform validate`でエラーがないことを確認
2. 実際にデプロイして動作を確認
3. `terraform destroy`でクリーンアップできることを確認

## コミットメッセージ

わかりやすいコミットメッセージを心がけてください：

```
[AZ900] 仮想マシンデモのTerraformテンプレートを追加

- Ubuntu VMのデプロイ設定
- ネットワーク構成を含む
- README.mdに手順を追加
```

## プルリクエスト

プルリクエストには以下を含めてください：

- 変更内容の説明
- テスト結果（該当する場合）
- スクリーンショット（該当する場合）
- 関連するIssue番号

## コードレビュー

すべてのプルリクエストはレビューを経て、承認された後にマージされます。
レビュー者は以下を確認します：

- ディレクトリ構造が正しいか
- ドキュメントが十分か
- コーディング規約に従っているか
- セキュリティ上の問題がないか

## 質問や提案

質問や提案がある場合は、Issueを作成してください。

## ライセンス

コントリビューションされたコードは、このリポジトリのライセンス（MIT）の下で公開されます。
